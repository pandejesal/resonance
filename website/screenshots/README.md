# Screenshots

Drop real UI captures here before launch. The homepage (`index.html`) references four slots
by these exact filenames — replace the placeholder boxes with real images named as below:

| Slot | File |
| --- | --- |
| Home & forgotten gems | `home-forgotten-gems.png` |
| Intelligence — decade mixes | `intelligence-decades.png` |
| Intelligence — sound-alikes | `intelligence-sound-alikes.png` |
| Now playing & waveforms | `now-playing-waveforms.png` |

## How to capture

1. Run the app against the demo server (or a local server with a filled library).
2. Capture 1600×1000 (16:10) PNGs — they render edge-to-edge in the shot grid.
3. Name them exactly as above and commit.

Do **not** ship the placeholder boxes on the live site — they are clearly marked as
placeholders in the HTML and will look unfinished to visitors.